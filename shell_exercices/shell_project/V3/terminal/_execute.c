#include "main.h"
/**
 * _execute - tokenize the command line
 * @command: entered line
 * @av: argument table
 *
 * Return: written line
*/
int _execute(char **command, char **av)
{
	return (NULL);
}
