#include "main.h"

int main(void)
{
	int i, j;

	for (i = 0; environ[i] != NULL; i++)
	{
		j = 0;
		while (environ[i][j] != '\0')
		{
			write(STDOUT_FILENO, &environ[i][j], 1);
			j++;
		}
		write(STDOUT_FILENO, "\n", 1);
	}
	return (0);
}
